# 🚗 Car Rental

**Car Rental** — mvp веб-сервиса для аренды автомобилей
Проект построен на **Flask** с **SQLAlchemy** и **PostgreSQL**, работает в **Docker**.

---

## 🧰 Стек технологий

| Компонент       | Технология                      |
| --------------- | ------------------------------- |
| Backend         | Python, Flask                   |
| ORM             | SQLAlchemy                      |
| База данных     | PostgreSQL (контейнер в Docker) |
| Шаблоны         | Jinja2 + HTML/CSS               |
| Контейнеризация | Docker, Docker-Compose          |
| Конфигурация    | `.env` для переменных окружения |

---
## Зависимости

Для запуска проекта требуется  [docker-desktop](https://www.docker.com/products/docker-desktop/)

## 🚀 Быстрый старт
1. **Склонируй репозиторий**
    
    ```bash
    git clone https://github.com/semyoonov/Car-rental.git
    cd Car-rental
    ```
    
2. **Скопируй и настрой `.env`**
    
    ```bash
    cp .env.example .env
    # отредактируй при необходимости
    ```
    
3. **Собери и запусти контейнеры**
    
    ```bash
    docker compose up --build
    ```
    

Приложение будет доступно по адресу `http://localhost:5000`.

---

## 📁 Структура проекта

```
Car-rental/
├── app.py                 # Flask-приложение
├── requirements.txt       # Python зависимости
├── docker-compose.yml     # Docker Compose конфиг (Flask + PostgreSQL)
├── web.dockerfile         # Dockerfile для приложения
├── templates/             # HTML-шаблоны (UI)
├── database/              # SQL миграции / init скрипты
├── .env.example           # Пример env переменных
├── .gitignore
└── README.md              # Этот файл
```

---